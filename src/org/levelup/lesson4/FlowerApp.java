package org.levelup.lesson4;

public class FlowerApp {
    public static void main(String[] args) {
        Flower flower = new Flower();
        // flower.name = "";
        flower.color = "";
        //this.name - обращение к текущему объекту (обращение к самому себя) this - единичный для каждого объекта (класса)
    }
}
